# Configuration file for analysis job submission
# Update parameters as needed to point to the paths you want to use
#
#jobs: 1 #number of jobs (required if no input files)
config: /nfs/slac/g/ldmx/users/mkilpatr/LDMX-scripts/batch/ecal_ana.py.tpl #full path to python config to be run (required)
prefix: Test_ucsb #string to attach to front of output root file (optional)
#event_output: #directory to cp event root files to after run (event and/or histogram output required)
histogram_output: /nfs/slac/g/ldmx/users/mkilpatr/LDMX-scripts/ecal_pn_validation #directory to cp histogram root files to after run (event and/or histogram output required)
input_dir: /nfs/slac/g/ldmx/data/mc/v12/4gev_1e_ecal_pn_val #directory of root files to process or lhe files to simulate over (optional) (default None)
job_dir: /nfs/slac/g/ldmx/users/mkilpatr/LDMX-scripts/jobs/ecal_pn_validation #directory to save job submission scripts to
#run_script: #full path to run script you want to use (default: $(pwd)/run_ana.py)
#batch_command: #command to submit a single job to the batch system (default: sbatch')
